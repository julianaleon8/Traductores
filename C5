using x, y of type integer begin
read x ;
read y ;
while x /= y repeat
if x > y then
x := x - y
else
y := y - x
done
done
{- El maximo común divisor ahora está en 'x' y en 'y'. {- El maximo común divisor ahora está en 'x' y en 'y'. -}-}
end
